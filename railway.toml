[build]
builder = "heroku/buildpacks:20"

[build.commands]
pre-build = "apt-get update && apt-get install -y libicu-dev && docker-php-ext-install bcmath intl"

[deploy]
startCommand = "php artisan serve --host=0.0.0.0 --port=$PORT"