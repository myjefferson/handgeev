[build]

commands.build = "composer install --optimize-autoloader --no-scripts --no-interaction && php artisan optimize"

[deploy]
startCommand = "php artisan serve --host=0.0.0.0 --port=$PORT"

[deploy.healthcheck]
path = "/"
timeout = 300